server:
  sources:
    - path: /downloads
      config:
        defaultEnabled: true
    - path: /music
      config:
        defaultEnabled: true
    - path: /podcasts
      config:
        defaultEnabled: true
    - path: /playlists
      config:
        defaultEnabled: true
