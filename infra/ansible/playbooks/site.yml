---
- name: Configure Alpine base
  hosts: alpine
  gather_facts: true
  roles:
    - common_alpine

- name: Configure Debian base
  hosts: debian
  gather_facts: true
  roles:
    - common_debian

- name: Configure edge LXC
  hosts: edge
  gather_facts: true
  roles:
    - caddy
    - ddns

- name: Configure DNS LXC
  hosts: dns
  gather_facts: true
  roles:
    - adguard_acme
    - adguard

- name: Configure tailnet LXC
  hosts: tailnet
  gather_facts: true
  roles:
    - tailscale_gateway

- name: Configure downloads LXC
  hosts: downloads
  gather_facts: true
  roles:
    - downloads_vpn
    - qbittorrent

- name: Configure files LXC
  hosts: files
  gather_facts: true
  roles:
    - copyparty

- name: Configure minecraft LXC
  hosts: minecraft
  gather_facts: true
  roles:
    - minecraft

- name: Configure hermes LXC
  hosts: hermes
  gather_facts: true
  roles:
    - hermes
