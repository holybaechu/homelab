---
- name: Restart nftables
  ansible.builtin.systemd:
    name: nftables
    state: restarted

- name: Restart wireguard
  ansible.builtin.systemd:
    name: "wg-quick@{{ proton_wg_interface }}"
    state: restarted
